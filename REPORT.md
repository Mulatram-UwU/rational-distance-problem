# 有理四距离问题（单位正方形内部）——推进报告

日期：2026-09-14（首轮），2026-09-15（第二轮：把搜索界从 `3×10⁹` 推到 `2×10¹⁰`，同日再推到 `5×10¹⁰`——分段可续跑，已完成）。
所有计算数字都来自本机实际运行的程序，原始输出保存在 `results/`；所有"已证明"的命题都有完整手写证明（`notes/reduction.md`），有限性部分另有 Lean 4 机器验证（`lean/`）。

> **作者与工作方式（重要声明）**：本仓库的**全部工作**——`src/` 里各版搜索引擎与暴力/独立枚举程序、`checks/` 里从定义出发的独立核对程序、`notes/` 里的手写证明与文献检索、`lean/` 与 `lean-mathlib/` 里的 Lean 4 形式化、`results/` 里记录的全部数值运行，以及本报告、`README.md` 和 `results/summary.md`——**均由 AI 研究助理 Agent「DeepSeek v4.1 flash」独立完成**，没有人类合作者。没有一行代码、一个证明、一次测量或一句结论出自人手。人类的介入仅限于四件事：提出要推进的问题、提供本机（Windows 笔记本，13th Gen Intel Core i5-13420H，8 核 12 线程）、规定工作纪律（不得编造结果；每个数字都必须能追到 `results/` 里的原始输出；不得放弃）、以及在发布时用 GPG 签名。详见 §11。

---

## 0. 摘要（先说结论）

问：单位正方形 `[0,1]²` 内部（不在边上）是否存在一点，使它到四个顶点的距离**全部**为有理数？

**本次工作没有解决这个问题。既没有找到这样的点，也没有证明它不存在。** 该问题在文献中是公开问题（Google DeepMind 的 `formal-conjectures` 仓库把它标记为 `research open`；见 §7）。

本次推进给出的、可核查的内容是：

0. **搜索界的第二轮推进（2026-09-15 新增）**：新引擎 v4/v5 解除了 v3 在 `N = 2×10¹⁰` 处的两个**硬**障碍（`40 GB` 的最小素因子表；`C²` 的 `u64` 溢出），并把除数游走的剪枝从"只在叶端生效"改成"在根端生效"（素数按从大到小访问），**穷尽**检验了所有"坐标共同分母 `≤ 2×10¹⁰`"的有理点——**无解**：`partners=161 541 752 656`、`candidates=7 650 247 495 045`、`tested=633 326 855 574`、`solutions=0`，12 线程跑 76 分钟（§2.1、§3.1）。其中 `partners` 与**完全不同的算法**独立计数（`src/paircount.cpp`）推出的 `2 × 80 770 876 328` **逐位相同**。中线专搜程序同时做了 128 位改写并推到同一个界（0 个解，§3.1）。**同日又用同一二进制把界推到 `N = 5×10¹⁰`：`partners=420 219 661 420`、`candidates=22 500 241 196 365`、`tested=1 845 867 547 562`、`solutions=0`，仍无解**；由于机器中途意外断电，这次采用分段可续跑的方式跑（24 段，各段自身搜索时间合计 2.88 小时），分段等价性已单独证明（§3.2）。
1. **计算搜索的推进（首轮主要成果）**：用一个新算法（毕达哥拉斯伙伴表配对法，把复杂度从 `O(N²)` 降到约 `Σ_C |T(C)|²`）**穷尽**检验了所有"坐标共同分母 `≤ 3×10⁹`"的有理点——**无解**（第二轮已把同一个界推到 `2×10¹⁰`，见上面第 0 条）。在"共同分母"这一自然参数下，本报告能核实的公开搜索界是 `10⁵`（自发布归档，见 §7），本工作把它推进到 `3×10⁹`（首轮）/ `2×10¹⁰`（第二轮完成）/ `5×10¹⁰`（第二轮延伸，**已完成**）——即四到五个半数量级。搜索被多套独立实现、独立暴力程序与独立计数程序多重交叉验证（§6）。
2. **一个结构定理**：任何解都满足 `12 | Q`（`Q` 为公共分母），且本原情形有强制的奇偶模式（定理 6 + 引理 4，证明见 §4）。其 3-adic 部分（`3|Q`）在文献中已有（McCloskey 2019，见 §7）；**2-adic 部分（`4|Q` 与奇偶模式）在我们检索到的文献中没有找到**，可能是新的，但我们无法确认这一点。**这两部分连同 `12|Q` 的完整合并，都已在核心 Lean 4 中机器验证**（§8）。
3. **新算法的正确性已在 Lean 4 中机器验证**：把搜索引擎所做的事逐条陈述并证明——伙伴集的除数刻画 `pyth_iff_div`、可靠性 `candidate_sound`、完备性 `exists_enum_of_sol`、奇偶过滤器的**无损性** `parity_normal_form`，以及主定理 `no_sol_of_no_candidate`。另用完全不同的算法在 Lean 里重算同一套计数器，用 `native_decide` 机器核对与 C++ 输出**逐位相同**（§8.2、§8.5）；第二轮进一步核对了 v4/v5 在 `N=1000,2000,3000` 上与 Lean 计数器逐位相同（§6）。
4. **几何桥（Mathlib）**：证明"几何命题（内部有理点、四距离全有理、共同分母 `≤ N`）⟺ 算法所用的整数方程组"（`hasRatPoint_iff`，§8.3），因此上面的定理说的确实是几何问题。
5. **中线 `A = B` 情形：一次更正 + 一项独立的冗余验证**（2026-09-15 更正）。上一版报告与 `lean/RationalDistance/Algorithm.lean` 的头注释声称"引擎枚举 `A < B`，因此**漏掉** `A = B`（即 `x = 1/2`），是真实覆盖缺口"。**这个说法是错的，现在更正**：把"反射 `x ↦ 1−x`"（交换 `A,B`，对 `A=B` 无效）与"对角线反射 `(x,y) ↔ (y,x)`"（交换 `(A,B) ↔ (C,D)`）混为一谈了。后者的确覆盖 `A = B`：把解按 `(A,B,C,D) ↦ (C,D,A,B)` 重排后，引擎框架里的"数对"变成 `(C,D)`，而 `C ≠ D`（否则 `A=B=C=D`，`A²+C² = 2A²` 是平方，与 `√2` 无理矛盾），于是引擎照常枚举到它。`notes/reduction.md` §6 从一开始就是**正确**的（那里写清了这一步），是报告与 Lean 注释写错了。
   因此：**中线专搜程序（`src/midline_search.cpp`）不是补缺口所必需，而是一重独立的冗余验证**——它用完全不同的方法（本原毕氏三元组参数化）独立确认了中线在同一个界上无解（`Q ≤ 2×10¹⁰`，1 462 708 696 个本原三元组，**0 个解**）。同一条中线上还得到一个**新的恒等式** `(C−D)⁴ + 4Q⁴ = 16h₁²h₂²`，把中线解归约到经典的 Fermat 直角三角形定理（`x⁴+4y⁴ = z²` 无非零整数解，§8.4）——这条归约本身是独立的结构性结果，与"是否补缺口"无关。
   Lean 侧对应的现状：`exists_enum_of_sol` 与 `no_sol_of_no_candidate` 里带了 `A ≠ B` / `hmid` 这两条**多余**假设（定理本身为真，只是假设比需要的强）。要拿掉它们，需要在 Lean 里补一个算术引理"`2A²` 不是正平方"（即 `√2` 无理的整数形式），再用上面的 `(A,B) ↔ (C,D)` 交换即可；这一点在 §8.7 的局限里明确列出。**注意这不影响任何计算结论**：两条假设都由实际计算满足，头条结论照旧成立。
6. **中心线 `x=1/2` 的独立等价刻画**（上一版成果）：归约到 genus-1 四次曲线 `ν² = λ⁴+8λ³+18λ²−8λ+1`（§5）。文献已用无限下降法证明中心线（乃至平面上整条中线）不可能（Yang Ji 2021，§7）；本次给出的是一条独立的、更"计算友好"的等价路径。

**环境更新（2026-09-15）**：GitHub 已恢复可达，**Mathlib 现已可用**——`mathlib4@master` 的 `lean-toolchain` 与本机已装工具链同为 `leanprover/lean4:v4.34.0-rc2`，`lake exe cache get` 取得官方预编译 olean，`lake build` 通过。上一版"Mathlib 不可用"的说明作废。项目结构：`lean/`（核心 Lean，无 Mathlib）+ `lean-mathlib/`（Mathlib 层，通过 `srcDir = "../lean"` 复用核心源码，全仓库每个源文件只有一份）。

---

## 1. 归约：几何问题 → 整数方程组

设 `P=(x,y)`，到 `V00,V10,V01,V11` 的距离为 `a,b,c,d`。

**定理 1（坐标必有理）.** `a,b,c ∈ ℚ` ⟹ `x,y ∈ ℚ`，且

```
x = (a² − b² + 1)/2 ,      y = (a² − c² + 1)/2 .
```

*证明.* `a² = x²+y²`、`b² = (1−x)²+y²` 相减得 `a²−b² = 2x−1`；同理 `a²−c² = 2y−1`。∎

> **归属说明（重要）**：这不是新观察。文献中多处明确陈述：Bremner–Ulas (arXiv:1502.07312) 的 Thm 2.1 证明中给出的公式在 `a=1` 时就是上式；McCloskey (arXiv:1904.12097) 摘要里直接写"任何到矩形四顶点均有理距离的点必有理坐标"；Love (arXiv:2310.02534)、Yang Ji (arXiv:2105.05250) 亦如此陈述。首次归属未找到，各处都当作初等事实。

取公共分母 `Q`（`Qx,Qy ∈ ℤ`），令 `A = Qx, C = Qy, B = Q−A, D = Q−C`。

**定理 2（整数等价形式）.** 对整数 `Q>0`、`0<A,B,C,D<Q`、`A+B = C+D = Q`：
`(A/Q, C/Q)` 到四顶点距离全有理 ⟺ `A²+C², B²+C², A²+D², B²+D²` 全为完全平方数。

*证明.* 四个平方距离为 `(A²+C²)/Q²` 等；有理数的平方若为整数必为整数（`(p/q)²=n, gcd(p,q)=1 ⟹ q²|p² ⟹ q=1`）。∎

**定理 3（搜索覆盖范围）.** 取 `Q = lcm(den x, den y)`，则"存在坐标共同分母 `≤ N` 的解" ⟺ "存在 `Q ≤ N` 且满足定理 2 的四元组"。因此 §3 的程序判定的恰是"**共同分母 ≤ N 时无解**"。

---

## 2. 搜索算法（为什么能跑到 2×10¹⁰）

记 `T(u) = { v : 1 ≤ v ≤ N, u²+v² 是完全平方 }`（毕达哥拉斯伙伴集）。定理 2 的条件等价于

```
A, B ∈ T(C)   且   A, B ∈ T(D),   其中 A+B = C+D = Q .
```

于是：

> 枚举 `C`；枚举 `T(C)` 中满足 `A<B`、`A+B ≤ N` 的数对；取 `Q = A+B`、`D = Q−C`；再检验 `D ∈ T(A)`、`D ∈ T(B)`——而 `D ∈ T(A)` 只需一次精确整数平方检验 `isSquare(A²+D²)`。

代价由 `O(N²)` 降为 `Σ_C |T(C)|²`。实测 `N=2×10¹⁰` 时候选数（即 `T(C)` 内满足 `A<B`、`A+B ≤ N`、`A+B > C` 的数对）为 `7 650 247 495 045 ≈ 7.65×10¹²`，而逐点扫描 `(x,y)` 是 `O(N²) = 4×10²⁰` 量级。下面四套实现（`src/`）互相逐项验证（v4 是 v5 的设计基线，v5 是头条运行用的那一个）：

| 版本                    | 伙伴表构造                                                      | 达到的 `N`                                       |
| ----------------------- | --------------------------------------------------------------- | ------------------------------------------------ |
| `fast_search.cpp` (v1)  | 三元组参数化，整表驻留内存                                      | `10⁸`                                            |
| `fast_search2.cpp` (v2) | 两遍枚举 + CSR 表（内存优化）                                   | `3×10⁸`                                          |
| `fast_search3.cpp` (v3) | **无全局表**：由 `C²` 的因子分解现算 `T(C)`；`u16` 最小素因子筛 | `3×10⁹`                                          |
| `fast_search4.cpp` (v4) | **分段筛 + 余项分解**；除数游走不构造 `C²`；按剩余类配对        | `10⁹`（与 v3 逐位对照过；设计基线）              |
| `fast_search5.cpp` (v5) | v4 再把 `C` 的素因子按**从大到小**访问，让剪枝在根端生效        | `2×10¹⁰`（头条运行）；`5×10¹⁰`（分段跑，已完成） |

v3 用的恒等式：`A²+C² = h² ⟺ (h−A)(h+A) = C²`，故 `T(C) = {(v−u)/2 : u·v = C², u<C, u ≡ v (mod 2)}`，由 `C` 的素因子分解深度优先枚举 `C²` 的因子即得。全部平方判定用精确整数平方根，不使用浮点。

### 2.1 v4/v5：两处硬限制、一处常数因子、一处剪枝位置

v3 到 `2×10¹⁰` 直接跑不动，有两个**硬**限制，都是 `N` 变大后才出现的：

1. **内存**：v3 把每个 `C ≤ N` 的最小素因子存进 `u16` 数组，`2×10¹⁰` 项即 `40 GB`；而且 `sqrt(2×10¹⁰) = 141423 > 65535`，`u16` 本身也不够用。
2. **溢出**：v3 的除数游走构造 `C*C`。`C = 2×10¹⁰` 时 `C² = 4.0×10²⁰ > 2⁶⁴`，默默回绕。（`N = 3×10⁹` 时 `C² = 9×10¹⁸ < 2⁶⁴` 恰好还没溢出——所以这个 bug 只在更远处才现形。）

v4 的改法：

* **分段余项筛**：`C` 按段 `[lo,hi)` 走，每段用"除尽所有 `p ≤ sqrt(hi)`，剩下的余项必为 `1` 或素数"做分解，峰值内存 `O(段长)`（`2²¹` 时约 `2.2 GB`，与 `N` 无关），不再需要全局表。
* **除数游走从不构造 `C²`**：`u | C², u ≤ C` 与它的补 `v = C²/u` 由*互补指数*同步乘出来（`v = ∏ p^(2e−f)`），两者都用 128 位比较封顶。封顶的依据是 `A = (v−u)/2 ≤ N ⟺ v ≤ 2N+u ≤ 3N`；某结点上补积已超过 `3N` 时，整棵子树都不可能产生 `A ≤ N` 的叶子（补积只随下降单调增大），可以直接剪掉。于是全程序**既不需要 128 位除法，也不需要大整数开方**。
* **按剩余类配对**（这是常数因子的来源，不影响判定）：v3 枚举 `T(C)` 的全部 `C(k,2)` 个数对，在循环内部丢掉约 94%。而引理 4（奇偶）与引理 5（`3|Q`）把 `(A mod 12, B mod 12)` 压到少数几个组合，并且**直接排除 `C ≡ 2 (mod 4)`**（引理 4 要求 `C ≡ 0 (mod 4)`，引理 5 的第二情形要求 `C` 为奇数）。v4 只走可能通过的那些数对。
* 平方检验改成"mod 64 先筛掉 81% → `double` 开方 → 128 位乘法校正"，`A²+D²` 与 `B²+D²` 短路求值。

**v5 的另一处：剪枝的位置。** v4 的 `v ≤ 3N` 剪枝在 `N=10⁹` 上只省下 4% 的结点（141.8e9 vs v3 的 148.0e9），
因为它按**递增**的素数顺序做除数游走、而补积 `∏ p^(2e−f)` 恰恰是被**大**素数推高的——等大素数轮到，
已经在树的深处，剪掉的子树很小。把顺序倒过来（大素数先决定）后，剪枝在根端就生效：同一 `N=10⁹` 上
结点数从 141.8e9 降到 **44.2e9**（3.2 倍），`N=10⁷` 上 895e6 → 339e6（2.6 倍）。枚举本身与顺序无关，
所以 `partners`/`candidates`/`tested` 一个都不变（§2.1 的表就是证据）。

`--no-mod3` 关掉引理 5、只留引理 4 的剩余类，正是 v3 的过滤器集合，用来逐数字对照两个引擎。

**与 v4 的对照实验**（`partners` / `candidates` / `tested` 三者必须逐个相同，只有 `divisor_steps` 因剪枝更强而略小）：

| `N`   | v3 (mod3 关)                              | v4 (mod3 关) | v3 (mod3 开)                         | v4 (mod3 开) |
| ----- | ----------------------------------------- | ------------ | ------------------------------------ | ------------ |
| 10⁴   | 28948 / 93740 / 19909                     | 相同         | 28948 / 93740 / 9626                 | 相同         |
| 10⁵   | 371728 / 2141875 / 421893                 | 相同         | 371728 / 2141875 / 211636            | 相同         |
| 10⁶   | 4539576 / 42803454 / 7954420              | 相同         | 4539576 / 42803454 / 4051329         | 相同         |
| 10⁷   | 53619848 / 781367361 / 138567430          | 相同         | 53619848 / 781367361 / 71198794      | 相同         |
| 10⁸   | —                                         | —            | 618449512 / 13346674090 / 1176085218 | 相同         |
| 10⁹   | 7006992014 / 216549264214 / 35765193862   | 相同         | —                                    | —            |
| 3×10⁹ | 22198268430 / 806162420921 / 131322339150 | 相同         | —                                    | —            |

`10⁹` 与 `3×10⁹` 两行是 v4/v5 与 `results/run_N1e9.txt`、`results/run_N3e9.txt` 里 v3 的**存档输出**
逐位相同（3e9 那次见 `results/run_v5_N3e9.txt`，12 线程 24.5 分钟；v3 当年是 25.7 分钟）。
`solutions` 在所有行都是 0。`sieve_overflow=0` 在所有运行里都成立。

**完备性（`A<B` 与奇偶剪枝为什么不丢解）**：见 `notes/reduction.md` §6。要点是 `(A,B)↔(B,A)`（即 `x↦1−x`）与 `(A,B,C,D)↔(C,D,A,B)`（即 `(x,y)↔(y,x)`）都保持四条件集合不变。奇偶剪枝的安全性由引理 4 的"折半下降"保证：任一解反复除以 2 直到不全偶，得到的仍是解，且 `Q` 更小，仍 `≤ N`。**`A=B`（即 `x=1/2`）也在覆盖范围内**：用对角线反射把解重排为 `(C,D,A,B)`，引擎框架里的数对就变成 `(C,D)`，而 `C≠D`（否则四个数全相等，`2A²` 会是平方，与 `√2` 无理矛盾）——详见 `notes/reduction.md` §6 与 §0 第 5 条的更正说明。引理 5（`3|Q`）是无条件必要的，其 3-adic 部分文献已有、`12|Q` 整体已在 Lean 中机器验证（§8.1）。

---

## 3. 计算结果

`N` 的含义：穷尽所有 `0<A,B,C,D<Q ≤ N`、`A+B=C+D=Q` 的四元组。"candidates" = 通过和/正性检验的数对 `(A,B) ⊆ T(C)` 数；"tested" = 通过奇偶剪枝、真正做四平方检验的数。

首轮（v1/v2/v3；"tested" 一律按 v3 的默认过滤器集合，即只做引理 4 的奇偶剪枝）：

| `N`   | 引擎 | 表项 / 因子步数        | candidates      | tested          | 解    | 墙钟             |
| ----- | ---- | ---------------------- | --------------- | --------------- | ----- | ---------------- |
| 10⁶   | v1   | 2 269 788 对           | 42 803 454      | 7 954 420       | **0** | 0.21 s           |
| 10⁷   | v1   | 26 809 924 对          | 781 367 361     | 138 567 430     | **0** | 4.0 s            |
| 10⁸   | v1   | 309 224 756 对         | 13 346 674 090  | 2 277 219 380   | **0** | 61.6 s           |
| 3×10⁸ | v2   | 1 973 076 898 表项     | 50 729 090 130  | 8 516 493 714   | **0** | 360.6 s          |
| 10⁹   | v3   | 148 039 211 084 因子步 | 216 549 264 214 | 35 765 193 862  | **0** | 633.3 s          |
| 3×10⁹ | v3   | 489 228 649 307 因子步 | 806 162 420 921 | 131 322 339 150 | **0** | 1540 s（25m40s） |

第二轮（v4/v5；`candidates` 与 v3 逐位相同，见 §2.1；`tested` 因过滤器集合不同而不同，
下面两行给出**同一引擎在两种过滤器下的两个数**，方便对读）：

| `N` | 引擎 | walk 结点数    | candidates      | tested（引理 4 单独 / 引理 4+5）         | 解    | 墙钟   |
| --- | ---- | -------------- | --------------- | ---------------------------------------- | ----- | ------ |
| 10⁸ | v5   | 3 903 016 895  | 13 346 674 090  | 2 277 219 380 / 1 176 085 218            | **0** | 15.7 s |
| 10⁹ | v5   | 44 199 282 253 | 216 549 264 214 | 35 765 193 862 / （见 `--no-mod3` 运行） | **0** | 218 s  |

原始输出：`results/run_N1e8.txt`、`run_N3e8.txt`、`run_N1e9.txt`、`run_N3e9.txt`、`run_N2e10.txt`。

**结论（计算性，不是证明）**：内部点、坐标共同分母 `≤ 2×10¹⁰` 时无解。等价说法：设 `x = p/q, y = r/s` 为既约有理数，若 `lcm(q,s) ≤ 2×10¹⁰`，则 `x²+y², (1−x)²+y², x²+(1−y)², (1−x)²+(1−y)²` 不可能全为有理平方。

### 3.1 头条运行 `Q ≤ 2×10¹⁰`（第二轮）

```
$ time ./bin/fast_search5.exe --N 20000000000
N=20000000000 divisor_steps=1018553868147 partners=161541752656 max_partners_per_C=10214 candidates=7650247495045 tested=633326855574 solutions=0 sieve_overflow=0
[search] 4558.14s total 4558.14s threads=12 mod3=on seg=2097152
```

读法：`partners` 是 `Σ_C |T(C)|`（所有 `(C,A)`，`A,C ≤ N`，`A²+C²` 平方）；`candidates` 是
和/正性检验通过的数对；`tested` 是**通过引理 4+引理 5 两个过滤器的数对**，即真正做四次平方检验的
次数；`divisor_steps` 是除数游走的结点数（v5 剪枝后的；v3 在同规模上不会更省）；`sieve_overflow=0`
表示分段筛里"每个 `C` 的不同小素因子数 ≤ 10"这一前提从未被突破（因此分解是完整的）。

三条独立核对都与这个输出对上：

* `partners = 161 541 752 656 = 2 × 80 770 876 328`，而后一个数来自**完全不同的算法**
  （`src/paircount.cpp` 的三元组参数化，`results/paircount_N2e10.txt`）；
* `12 | Q` 与定理 6：`tested` 里所有数对都满足 `12 | A+B`（由过滤器的构造保证）；
* 同一二进制在 `N = 10⁴…10⁹` 上与 v3 存档输出逐位相同（§2.1、§6 第 11 条）。

墙钟 76 分钟（本机为 13th Gen Intel Core i5-13420H，8 核 12 线程；峰值内存约 2.2 GB，与 `N` 无关）。

### 3.2 第二轮延伸：`Q ≤ 5×10¹⁰`（分段可续跑；**已完成**）

**结果**（存档 `results/run_N5e10.txt`，由 24 段的输出逐字段求和写出；每段自己的原始输出在
`results/chunks5e10/part_kk.txt`，进度在 `part_kk.err`）：

```
N=50000000000 divisor_steps=2649313994389 partners=420219661420 max_partners_per_C=14317 candidates=22500241196365 tested=1845867547562 solutions=0 sieve_overflow=0
```

即**所有"坐标共同分母 `≤ 5×10¹⁰`"的有理点都不满足四距离全有理——无解**。24 段自身的搜索时间合计
**10 366.35 s（2.88 小时）**；从第一段启动（13:38）到汇总行写出（16:40）的墙钟为 3.0 小时（第一段与
`paircount` / `midline` 两个同伴程序共用机器，故略慢）。核对项全部通过：

* `partners = 420 219 661 420` 恰好等于 `2 × 210 109 830 710`，而 `210 109 830 710` 来自
  `src/paircount.cpp`——**完全不同的算法**（三元组参数化）在同一界上的独立计数
  （`results/paircount_N5e10.txt` 自带一句 `v3 'partners' must equal 2*pairs = 420219661420`）；
* `solutions = 0`；`sieve_overflow = 0`（后者表示分段筛"每个 `C` 的不同小素因子数 ≤ 10"这一前提
  从未被突破，因此分解是完整的）；
* 对角线 `x = y` 由独立程序另跑一遍：`results/midline_N5e10.txt` 给出
  `Amax=25000000000 primitive_triples=3656771176 midline_solutions=0`（80.87 s）；
* 24 段的输出**逐字段相加等于上面这一行**，`checks/verify_results.py` 的 5e10 分支直接断言这一点
  （该脚本现在共 20 项断言，全部 PASS，见 `results/verification.txt`）。

下面是这次运行是怎么被做成"可续跑"的，以及为什么它的代价分布和最初估计不同。

2e10 用 76 分钟；按同一二进制在 `10⁸ → 10⁹ → 2×10¹⁰` 上的实测伸缩，`5×10¹⁰` 当时估计约需 3～5 小时（**事后实测为 2.88 小时**，早期估计偏大，原因见下文与 `notes/cost-5e10.md`）。
第一次尝试用**整跑**形式（`bin/fast_search5.exe --N 50000000000`）在 2026-09-15 10:32 启动，
跑到 11000/23842 段时机器意外断电。整跑只在**结束时**打印计数器，而 stdout 重定向到文件后是块缓冲的，
于是那一半工作量连同输出一起丢失——当时 `results/run_N5e10.txt` 是 0 字节（这个空文件后来被本次成功运行的汇总行覆盖），
只有 stderr 的进度行留了下来（留档：`results/attempt1_5e10_interrupted.err`）。

为此给 v5 增加了 `--seglo` / `--seghi`（只跑段号 `[lo, hi)`）。引擎对每个 `C` 的处理与它落在哪一段无关，
且它打印的每个计数器都是对 `C` 的求和（`partners`、`candidates`、`tested`、`solutions`、`divisor_steps`、
`sieve_overflow` 可加，`max_partners_per_C` 取最大），所以分段跑完把各行相加**就是**整跑会打印的那一行。
`checks/verify_chunking.sh`（输出 `results/verify_chunking.txt`）把这一点做实，三项检查全部通过：

* `N = 10⁸` 整跑 vs 同一 `N` 切成三段之和：**逐位相同**（含 `divisor_steps`，即每一段的工作量也相加吻合）；
* `N = 10⁸`、`10⁹` 的整跑与 v3 存档（`results/run_v3_N1e8.txt`、`results/run_N1e9.txt`）相比，
  `partners` / `max_partners_per_C` / `candidates` / `tested` / `solutions` **逐个相同**，
  只有 `divisor_steps` 更小（1e8：3.90×10⁹ vs 1.19×10¹⁰；1e9：4.42×10¹⁰ vs 1.48×10¹¹），与 §2.1 一致。

现在的运行方式是 `run_5e10.sh`（等价入口 `bash run_all.sh 5e10`）：把 23842 段按**估算工作量**
（≈`C^1.8`）而不是按 `C` 区间切成 24 份（段界 0, 4079, 5995, … , 23842）；
每份的原始 stdout/stderr 留在 `results/chunks5e10/part_kk.{txt,err}`，已经完成的份在再次运行脚本时自动跳过，
24 份齐了自动相加写入 `results/run_N5e10.txt`（同时留下 `results/chunks5e10/pieces.txt` 这一份"24 行原始输出"的拼接，供脚本逐字段求和）。

**分段按时间并不均匀**（这一点事后才知道，写在这里以免误读）：估算用的指数 `C^1.8` 是偏大的，真实代价随 `C` 衰减得慢得多（近乎持平再缓降），所以第 00 段 `[0,4079)` 花了 3932 s，而最后一段 `[23285,23842)` 只用 110.91 s。24 段的实测耗时表（0.964 → 0.533 → … → 0.111 s/段）与更正后的总耗时估计写在 `notes/cost-5e10.md`，原始探针输出在 `results/probe_cost_5e10.txt`。这不影响任何结论：每段只被访问一次、总和不变，分段模式与整跑逐位等价（`checks/verify_chunking.sh`）。代价集中在**小 `C`**：`candidates` 从每段 5.3×10⁹（`C ≈ 10⁷`）降到 8.3×10⁷（`C ≈ 4.2×10¹⁰`），所以第 00 段是最贵的一段而不是最便宜的；但即便如此，第 00 段一段的 `tested=1 094 855 716 543` 就已超过整个 `2×10¹⁰` 运行的 `633 326 855 574`。

同一界上的两个独立对照（`src/paircount.cpp` 的三元组参数化计数、`src/midline_search.cpp` 的对角线穷尽）
在本次搜索进行期间就已经跑完，它们不是"搜索结果"，而是对搜索结果的**约束**——具体数字见本节开头的核对清单。

**5e10 现已计入本报告的结论**（此前几版写着"不计入"，是因为它当时还在跑）：本报告主张的搜索界是
`Q ≤ 5×10¹⁰` 无解，由 §3.1 的 `2×10¹⁰` 整跑与本节 `5×10¹⁰` 的分段跑共同支持。这一提升的代价是
"界再抬 2.5 倍"，收益是搜索空间再扩大 2.5 倍；**它仍然不是证明**——更高分母的 `Q` 依然没有被排除，
问题依然公开。

---

## 4. 结构定理：`12 | Q`

**引理 4（模 8／奇偶结构）.** 若四条件成立且 `A,B,C,D` 不全为偶，则或 (P1) `A,B` 奇且 `4|C,4|D`，或 (P2) `C,D` 奇且 `4|A,4|B`；两种情形都有 `4|Q`。

*证明.* 模 8 的平方只有 `0,1,4`。四个对角对 `(A,C),(A,D),(B,C),(B,D)` 不能同时为奇（否则平方和为 `2 mod 4`，非平方剩余）。若 `A` 奇，则 `C,D` 偶，由 `1+C²` 必须是模 8 平方得 `C² ≡ 0 (mod 8)`，即 `4|C`；同理 `4|D`，于是 `Q = C+D ≡ 0 (mod 4)`，且 `B = Q−A` 为奇。若 `C` 奇则对称得 P2。无人为奇则全偶，与假设矛盾。∎

**引理 5（模 3）.** 四条件与 `A+B=C+D=Q` ⟹ `3 | Q`。

*证明.* 模 3 平方为 `0,1`。若 `3∤A`，由 `A²+C²` 是模 3 平方得 `3|C`；同理 `3|D`，故 `3|Q`。若 `3|A` 而 `3∤B`，对 `B` 同理得 `3|C,3|D`，于是 `Q≡0` 但 `Q = A+B ≡ B ≢ 0`，矛盾；故 `3|A ⟹ 3|B`，于是 `3|Q`。∎

> **归属说明**：引理 5 与文献中 McCloskey (arXiv:1904.12097, 2019) 的结论 `v₃(x)<0 或 v₃(y)<0` 是同一件事（`3|Q ⟺ 3|den x 或 3|den y`）。**该结论已有，我们只是给出一个更短的证明。**

**定理 6.** 四元组满足定理 2 的条件 ⟹ `12 | Q`。

*证明.* 引理 5 给 `3|Q`。若四元组不全偶，引理 4 给 `4|Q`。若全偶，则 `(Q/2, A/2, B/2, C/2, D/2)` 仍是正整数解（条件齐次，整体缩 4 倍），反复折半直到不全偶，由引理 4 得 `4 | Q/2^k`，故 `4 | Q`。∎

**推论.** 任何解的最小公共分母必被 12 整除。

**`12` 在局部论证下是最优的.** `src/residue_structure.py` 对每个模 `m` 穷举所有满足 `A+B ≡ C+D` 且四个模 `m` 平方剩余条件的剩余四元组：

| `m`                     | 可能的 `Q mod m` | 强制   |
| ----------------------- | ---------------- | ------ |
| 4                       | 0, 2             | 仅 2   |
| 8                       | 0, 2, 4, 6       | 仅 2   |
| 16                      | 0, 4, 8, 12      | 4      |
| 3                       | 0                | 3      |
| 9, 27                   | 3 的倍数         | 3      |
| 12                      | 0, 6             | 6      |
| 144                     | 12 的倍数        | 12     |
| 5, 7, 11, 13, 25, 49    | 全部             | 无     |
| **24 + 不全偶（本原）** | **0, 12**        | **12** |
| 48, 96, 144, 288 + 本原 | 12 的倍数        | 12     |

局部论证永远推不出比 `12|Q` 更强的结论；而对**本原**四元组，`12|Q` 甚至可由模 24 的有限检查直接证明（这正是 Lean 形式化的目标之一）。

---

## 5. 中心线 `x = 1/2`：到 genus-1 曲线的等价刻画

`x=1/2` 时 `A=B`，四条件塌缩为 `A²+C²`、`A²+D²` 为平方且 `C+D = 2A`。令 `u = C/A ∈ (0,2)`，等价于：`1+u²` 与 `1+(2−u)²` 同时为有理平方。

用 `1+u² = w² ⟺ u = (1/λ−λ)/2`（`λ = w−u ≠ 0`）参数化，两条相加给出关于 `μ` 的二次方程，判别式为

```
Δ(λ) = λ⁴ + 8λ³ + 18λ² − 8λ + 1 ,
```

于是中心线解 ⟺ `ν² = Δ(λ)` 的（满足区间条件的）有理点。另有精确恒等式

```
Δ(λ) = λ² (v² + 8v + 20),   v = λ − 1/λ ,
```

说明该曲线由两个二次型条件（`v²+4=□` 与 `v²+8v+20=□`）刻画，是一条 genus-1 曲线。

**计算**（`src/center_line.py`；第二轮未改动本节的任何数字）：

- 恒等式与归约在 2000 个随机有理 `λ` 上精确验证；归约的代数正确性另在 12 712 个有限域实例（`p ∈ [101,400)`）上验证，**0 反例**。
- 穷尽搜索 `λ = p/q`（`|p|,q ≤ 1500`）的曲线有理点：**0 个**。
- 直接穷尽搜索 `u = p/q ≤ 1500`：**0 个**（与引擎在 `Q ≤ 10⁹` 上的结论一致）。

### 5.1 本次新增：更直接的整数归约 + 中线穷尽搜索

上一版走的是 genus-1 四次曲线；本次给出一个**更短**的归约（已在 Lean 中机器验证，§8.4）：
中线解满足 `Q = 2A`、`C + D = 2A`、`A²+C² = h1²`、`A²+D² = h2²`，代入 `Q = 2A` 并令 `C = 2A − D`，纯代数给出恒等式

```
(C − D)^4 + 4 Q^4 = 16 h1² h2²        （数值自检：A,C,D = 5,1,9 → 44096 = 16·26·106 ✓）
```

配合 `2A²` 非正平方（故 `C ≠ D`），得到：**一个中线解会给出非零整数 `x = C−D`、`y = Q`、`z = 4h1h2` 满足 `x⁴ + 4y⁴ = z²`**——即一个面积为平方数的整数直角三角形。这正是经典的 **Fermat 直角三角形定理**（等价于 `FLT(4)`、等价于"1 不是同余数"）。

**中线穷尽搜索（本次新增，`src/midline_search.cpp`）**：主搜索引擎枚举 `A < B` 的数对；如 §0 第 5 条所更正，`A = B` 其实也落在它的覆盖范围内（用对角线反射重排即可），所以这个程序**不是补缺口所必需**，而是一重**独立的冗余验证**——它用完全不同的方法（本原毕氏三元组参数化）独立地把整条中线扫一遍，两个方法必须一致。归约：中线解经本原化后 `gcd(A,C,D) = 1`，此时 `A` 必为偶（否则 `A=B` 奇 ⟹ `4|C,4|D` ⟹ `4|Q=2A`，矛盾），于是 `(A,C)` 是偶数股为 `A` 的本原毕氏三元组，`A = 2mn`、`C = m²−n²`（`gcd(m,n)=1`，`m>n`，`m−n` 奇），且**全部条件对缩放齐次**，故只需跑本原三元组。

```
./bin/midline_search.exe 20000000000
N=20000000000 Amax=10000000000 primitive_triples=1462708696 isqrt_tests=1462708696 midline_solutions=0
[midline] 26.27s
```

即：**中线在共同分母 `Q ≤ 2×10¹⁰` 内无解**（1 462 708 696 个本原三元组，26 秒）。第二轮的改动是必要的 128 位改写：`N=2×10¹⁰` 时 `A ≤ 10¹⁰`、`D < 2A`，`A²+D²` 约 `5×10²⁰ > 2⁶⁴`，原来的 `u64` 会溢出（`N=3×10⁹` 时 `A²+D² ≤ 1.1×10¹⁹ < 2⁶⁴` 恰好还没溢出，所以这个 bug 只在更远处现形）；同时把 `m` 的循环用 `D > 0` 的等价条件 `m < (2+√5)n` 收紧，使 `m²` 始终留在 `u64` 内。

验证：(i) 改写后的程序在 `N=3×10⁹` 上**逐位复现**改写前的输出（`primitive_triples=219406350`，0 解）；(ii) 非平凡的**阳性对照**——对 `Amax = 50000`，不依赖参数化的直接暴力计数（枚举所有本原 `(A,C)`，`A` 偶、`gcd(A,C)=1`、`C<2A`、`A²+C²` 平方）得 **7312**，参数化枚举器报 `primitive_triples=7312`，**完全相等**（`checks/midline_ref.cpp`）；(iii) 早前对 `A ≤ 20000` 的双向集合相等核对（3573 = 3573）。

> **归属与状态说明**：**中心线（乃至平面上整条中线）不可能，文献已证**——Yang Ji (arXiv:2105.05250, 2021) 用 Fermat 无限下降法证明了点落在对角线、中线或边上时四距离不可能同时有理，并明确指出"该证明可推广到整个平面，而不只限于正方形内部"。因此本节的归约不是新结果，而是**两条独立的、可计算化的等价刻画**（与文献结论一致，并可用来做有限域/有理点/穷尽搜索）。我们没有给出"中心线整体无解"的独立**证明**：§8.4 的 Lean 归约把问题**精确地**化到 Fermat 直角三角形定理（在 Mathlib 里该定理只有 `FLT(4)` 的对应形式，从后者推出本形式需要与 `FLT(4)` 证明中相同的无穷下降，本次未完成）；本节的结论（`Q ≤ 2×10¹⁰` 无中线解）是**计算性**的，与上一条一样，不构成整体不存在性的证明。**并且按 §0 第 5 条的更正，中线无解对主搜索的完整性而言是冗余的**（引擎的覆盖已含 `A=B`），这里的作用是"两种完全不同的方法给出同一个 0"。

---

## 6. 独立验证（多重交叉检查）

1. **三个引擎互相一致**：`candidates`、`tested`、`solutions` 在 `N = 10⁴…10⁷` 完全逐项相同（`results/consistency.txt`），`N=10⁸` 亦一致。三者表构造方式完全不同（v3 完全不用表）。
2. **独立暴力程序**（子代理另写：`src/brute.cpp`，直接三重循环扫描 `(Q,A,C)`）：`Q ≤ 5000` 无解；`Q ≤ 10⁴` 也无解（3332.8 亿次候选检验，335.8 s，12 线程）。另有"允许点落在边上"的 `--symmetric` 模式，同样无解。
3. **独立伙伴对枚举器**（`src/brute_pairs.cpp`，与三元组参数化不同的算法）：在 `N=200000` 上与我方枚举产生**完全相同的集合**（396 481 个无序对，双向差集 0）。
4. **放宽问题的"打靶"检验（小规模，逐元组）**：把问题放宽为只要求 `A²+C², B²+C², A²+D²` 为平方（去掉第四条件）并限定 `A<B`，则 `N=300` 时引擎输出与独立 Python 三重循环枚举**逐元组相同**（各 6 个解，无多无少）。
5. **放宽问题的"打靶"检验（大规模）**：同一引擎在上述放宽条件下找到大量解——`N=10⁵` 时 **3 423** 个，`N=10⁶` 时 **35 065** 个；加上第四条件 `B²+D²` 后，`N ≤ 10⁹` 全部为 **0**。这排除了"引擎因某环节失效而永远输出空集"的解释：它在有解时确实命中，且命中量随 `N` 正常增长。
6. **表规模独立核对**：`src/paircount.cpp` 用三元组参数化独立计数无序毕氏对数，与 v3 由因子分解生成的 `partners` 统计严格吻合：
   `N=10⁹`：3 503 496 007 ×2 = 7 006 992 014 ✓；
   `N=3×10⁹`：11 099 134 215 ×2 = 22 198 268 430 ✓（与那次头条运行的输出逐位相同）。
   这独立验证了 v3 伙伴表在头条规模上的正确性（`N=10⁶,10⁸` 亦吻合）。
7. **剪枝过滤器的无害性**：`--no-parity` 与默认、`--mod3` 与默认，在所有测试过的 `N` 上结果完全一致。
8. **中线（`A = B`）独立穷尽搜索**（`src/midline_search.cpp`）：如 §0 第 5 条所更正，主引擎其实**也覆盖** `A = B`（借对角线反射重排），所以这是一个**冗余的独立验证**：用完全不同的方法（本原毕氏三元组参数化）把整条中线单独扫一遍，在 `N = 2×10¹⁰` 上跑了 1 462 708 696 个本原三元组、**0 个解**（26 s，`results/midline_N2e10.txt`），在 `N = 5×10¹⁰` 上跑了 3 656 771 176 个、**0 个解**（80.87 s，`results/midline_N5e10.txt`）。枚举正确性经两重核对：改写后在 `N=3×10⁹` 上与改写前输出逐位相同；`Amax=50000` 时与不依赖参数化的直接暴力计数**完全相等**（7312 = 7312）。
9. **Lean 模型 vs C++ 计数器交叉核对**（`RationalDistance/Count.lean`）：用**完全不同的算法**（对每个 `A ≤ N` 直接 `Nat.sqrt` 判平方，而不是用 `C²` 的除数）在 Lean 里重算 `(partners, candidates, tested, solutions)`，`native_decide` 核对与 C++ 打印**逐位相同**（`N = 1000, 2000, 3000`，§8.5）。第二轮进一步确认 **v4/v5 也逐位复现这三个 Lean 计数器**（§2.1）。
10. **引理 4 的剩余类层面独立验证**：穷举模 `m` 的剩余四元组（要求 `A+B≡C+D` 与四个平方剩余条件），剔除"全偶"模式后，`Q mod 8 ∈ {0,4}`、`Q mod 16 ∈ {0,4,8,12}`，即模 8 已强制 `4|Q`（模 4 单独做不到，允许 `Q≡2`，与引理 4 的陈述一致）。
11. **v4 / v5 对 v3 的逐位对照**（第二轮新增）：新引擎在 `N = 10⁴, 10⁵, 10⁶, 10⁷` 上、两种过滤器集合下，`partners` / `max_partners_per_C` / `candidates` / `tested` / `solutions` 与 v3 **全部逐位相同**；`N = 10⁸` 同上；`N = 10⁹` 与 `results/run_N1e9.txt` 的存档输出逐位相同（`partners=7006992014`、`candidates=216549264214`、`tested=35765193862`）；`N = 3×10⁹` 与 `results/run_N3e9.txt` 的存档输出逐位相同（`partners=22198268430`、`candidates=806162420921`、`tested=131322339150`）。差别只在 `divisor_steps`/`walk_nodes`（v4/v5 的剪枝更强：3e9 上 1.40×10¹¹ vs v3 的 4.89×10¹¹）。
12. **伙伴表规模独立核对（头条规模）**：`src/paircount.cpp`（三元组参数化，与引擎的因子分解方法完全不同）在 `N = 2×10¹⁰` 上数出无序毕氏对 **80 770 876 328**，因此引擎的 `partners` 必须是 **161 541 752 656**（`results/paircount_N2e10.txt`）；头条运行的实测值正好是 `161541752656`，**逐位吻合**。同一核对在 `N=10⁶,10⁸,10⁹,3×10⁹` 上都吻合。此外 `sieve_overflow=0`：分段筛里"每个 `C` 的不同小素因子数 ≤ 10"这一前提从未被突破，因此所有分解都完整（这直接决定 `partners` 的完整性）。
13. **`5×10¹⁰` 延伸的交叉核对（第二轮新增）**：(a) **分段等价性**——`N = 10⁸` 整跑与它的三段之和**逐位相同**（含 `divisor_steps`，即每段的工作量也相加吻合），且 `10⁸`/`10⁹` 整跑与 v3 存档逐位相同（`checks/verify_chunking.sh` → `results/verify_chunking.txt`，0 项失败）；(b) **分段求和**——24 段的输出逐字段相加等于汇总行 `results/run_N5e10.txt`；(c) **独立计数**——`src/paircount.cpp` 在同一界上数出无序毕氏对 **210 109 830 710**，故 `partners` 必须是 **420 219 661 420**，而实测值正好是 `420219661420`；(d) **中线**——独立穷尽 `N = 5×10¹⁰`：3 656 771 176 个本原三元组，0 个解；(e) `solutions=0`、`sieve_overflow=0`。以上五条都由 `checks/verify_results.py` 自动断言（现共 20 项，全部 PASS）。

---

## 7. 与文献的关系（由独立子代理检索，逐条附 URL；见 `notes/literature.md`）

**问题状态**：公开问题。Google DeepMind 的 `formal-conjectures` 仓库把它标记为 `@[category research open]`（`FormalConjectures/Wikipedia/RationalDistanceProblem.lean`）。且文献中该问题的标准提法是**整个平面**（Bremner–Ulas 摘要；Yang Ji 第 1 节），而非仅内部。

**已有的严格结论（对我们最相关的三条）**

- **边界点不可能**：Barbara, *Math. Gazette* 95 (532) 2011, 59–61。
- **对角线／中线／边上不可能**，并推广到整个平面：Yang Ji, arXiv:2105.05250 (2021)——用 Fermat 无限下降法。
- **3-adic 障碍**：McCloskey, arXiv:1904.12097 (2019)：对单位正方形，任何解必须满足 `v₃(x)<0` 或 `v₃(y)<0`（等价于本报告的 `3|Q`）。
- 另：Robertson 1986（*Math. Magazine* 59, p.52）报称"点到两条边的距离相等时无解"，但**从未发表证明**；`v₃` 筛掉约 1/4 的候选。

**已知的搜索界（务必注意表述差异）**

- 我们能**直接核实**（拉取了代码/README）的最强界：自发布归档 `github.com/SamSi0322/rational-distance-unit-square`（及 Zenodo 19613346/19613981，2026，未同行评议）：在 `x=p/q, y=r/q, gcd(p,r,q)=1` 的**共同分母**表述下，**`q ≤ 100,000` 穷尽无解**；其正文明确写"不声称完整解，问题仍开放"。另有基于"毕氏腿 ≤ 5×10⁶"的搜索（其 v1.1 更新日志说明该轮不是分母界）。
- 检索中出现的**最大声称**：Tim S Roberts 在 groups.io `UnsolvedProblems` 论坛（topic 67887461）称 (1) 边长 `< 6×10⁶` 的正方形内部无解；(2) 边长与坐标都 `< 2×10⁶` 时（内部或外部）无解。**该页面被 JS 反爬挡住，两种方式都取不到正文，故属"未核实"**，且作者自述程序可能有 bug。
- MSE 1379191（Yimin Rong, 2015）：在**约化后的**子问题（两毕氏三元组 `(L,a,p),(L,b,q)` 且 `a+b=L`）上验证到 `L<2³¹`——这不是完整的内部问题界，不能当作同一件事引用。
- **与我们的对比**：在同一个"共同分母"参数下，可核实的公开界是 `10⁵`，我们做到 `10⁹`（提高 4 个数量级，且我们的搜索是穷尽式的、覆盖全部有理点而非整点特例）。上面 Roberts 的第 (2) 条若按"整数坐标"理解，则被我们的结果包含；若按"坐标的分子分母各自 < 2×10⁶"理解，则两者覆盖范围不可互相包含（共同分母界与独立分子分母界是两种不同的参数化），此时我们的界不能声称涵盖它。

**声称的完整证明**：仅检索到 Song Li, viXra:2605.0013（2026）声称证明了内部无解（"分三种奇偶情形导出矛盾"）。**不可信**：viXra（无同行评议）、非 arXiv、且该页面自带 viXra 管理员留言"Please submit article written with AI assistance to ai.viXra.org"。arXiv 上无任何声称解决该问题的论文。**本报告不引用该"证明"的任何内容。**

**与本报告其它结论的关系**

- 定理 1（坐标有理）、引理 5（`3|Q`）、中心线不可能：**均为文献已有**（见上），本报告只提供独立/更短的证明或等价刻画，不主张原创。
- 定理 6 的 **2-adic 部分**（`4|Q`、本原奇偶模式 P1/P2）：在上述检索到的文献中**没有找到**相应陈述。可能是新的，但检索不可能穷尽，故我们只写"未在检索到的文献中发现"，不作原创性断言。
- 我们的计算搜索规模（共同分母 `10⁹`）超出可核实的公开界 4 个数量级。

---

## 8. Lean 4 机器验证

**环境更新（2026-09-15）**：GitHub 已恢复可达，**Mathlib 现已可用**：`lake update` 克隆
`mathlib4@master`（其 `lean-toolchain` 与本机已装的 `leanprover/lean4:v4.34.0-rc2` **完全相同**），
`lake exe cache get` 取得官方预编译 olean，`lake build` 通过。因此本次工作有两套 Lean 工程：

| 工程            | 依赖        | 内容                                            |
| --------------- | ----------- | ----------------------------------------------- |
| `lean/`         | 仅核心 Lean | `12 \| Q`、**搜索算法的正确性**、计数器交叉核对 |
| `lean-mathlib/` | Mathlib     | **几何桥**、**中线归约**、主定理拼装            |

`lean-mathlib/lakefile.toml` 用 `srcDir = "../lean"` 把核心工程接入，故全仓库每个源文件只有一份。
上一版报告中"Mathlib 不可用"的说明已作废；§8.1 中凡当时只能用核心 Lean 完成的部分保持不变，仍可编译。

### 8.1 `12 | Q`（核心 Lean；与上一版相同）

构建命令 `lake.exe build`（在 `lean\` 下）：

| 文件                              | 定理                                       | 内容                                                   |
| --------------------------------- | ------------------------------------------ | ------------------------------------------------------ |
| `RationalDistance/ModArith.lean`  | `sq_mod8` / `sq_mod3`                      | 平方 mod 8 ∈ {0,1,4}；平方 mod 3 ∈ {0,1}               |
|                                   | `odd_left_forces`                          | `A` 奇且 `A²+C²` 为平方 ⟹ `4 ∣ C`                      |
|                                   | `three_dvd_Q`                              | 四条件 + `A+B=C+D=Q` ⟹ **`3 ∣ Q`**（引理 5）           |
| `RationalDistance/Parity.lean`    | `not_both_odd` / `four_dvd_of_odd_partner` | `A,C` 不同时为奇；`X` 奇且 `X²+Y²` 为平方 ⟹ `4 ∣ Y`    |
|                                   | `four_dvd_Q`                               | 四条件 + 不全偶 ⟹ **`4 ∣ Q`**（引理 4 的 2-adic 结论） |
| `RationalDistance/TwelveDvd.lean` | `sq_of_four_mul_add` 等                    | 折半下降的技术引理                                     |
|                                   | **`twelve_dvd_Q`**                         | **定理 6：任何满足四条件的四元组都有 `12 ∣ Q`**        |
| `RationalDistance/Search.lean`    | `no_solution_Q_le_100`                     | `native_decide`：`Q ≤ 100` 无解                        |
| `BigCheck500.lean`（项目根）      | `no_solution_Q_le_500`                     | `native_decide`：`Q ≤ 500` 无解                        |

`#print axioms` 实测：上述结论只依赖 `[propext, Classical.choice, Quot.sound]`，无 `sorry`、无额外公理。

### 8.2 搜索算法的正确性（核心 Lean，本次新增；`RationalDistance/Algorithm.lean`）

这是本次形式化的主体：把 `src/fast_search*.cpp` 所做的事情**逐条**在 Lean 里陈述并证明。
文件头部定义（全部是 `Prop`，不含计算）：

```
Pyth C A          := ∃ h, A*A + C*C = h*h            -- A 是 C 的毕氏伙伴
IsSol N A B C D Q := A+B = Q ∧ C+D = Q ∧ 0<A,B,C,D ∧ Q ≤ N
                     ∧ Pyth C A ∧ Pyth C B ∧ Pyth D A ∧ Pyth D B
ParityOK A B C D  := (A,B 奇 ∧ 4|C ∧ 4|D) ∨ (C,D 奇 ∧ 4|A ∧ 4|B)   -- 程序的过滤器
Enum N C A B      := 0<A ∧ 0<C ∧ Pyth C A ∧ Pyth C B ∧ A<B ∧ A+B ≤ N
                     ∧ C < A+B ∧ Pyth (A+B-C) A ∧ Pyth (A+B-C) B
Candidate N C A B := Enum N C A B ∧ ParityOK A B C (A+B-C)          -- 程序真正枚举的东西
```

证明的定理（全部编译通过，`Build completed successfully`）：

| 定理                                 | 陈述                                                                                                                  | 作用                                                                                                                                                                                 |
| ------------------------------------ | --------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **`pyth_iff_div`**                   | `0<C`、`0<A` 时：`Pyth C A ↔ ∃ u v, u*v = C*C ∧ u < C ∧ u + 2*A = v`                                                  | **算法的心脏**：证明"枚举 `C²` 的除数 `u`、令 `v = C²/u`、`A = (v−u)/2`"恰好产生全部伙伴。这正是 `fast_search3.cpp` 的除数 DFS。（手写证明只有两行代数：`u*(u+2A) + A² = (u+A)²`。） |
| `candidate_sound`                    | `Candidate N C A B ⟹ IsSol N A B C (A+B-C) (A+B)`                                                                     | **可靠性**：程序报出的每个四元组都真是解。                                                                                                                                           |
| `sol_swap`                           | `IsSol N A B C D Q ⟹ IsSol N B A C D Q`                                                                               | 反射 `x ↦ 1−x` 交换 `A,B`。                                                                                                                                                          |
| `enum_of_sol` / `exists_enum_of_sol` | 若 `IsSol` 且 `A ≠ B`，则存在交换后的解满足 `Enum`                                                                    | **完备性（无过滤器部分）**：`A < B` 的限制由反射对称性免费补偿。**注**：这里的 `A ≠ B` 假设是**多余**的——见下方说明。                                                                |
| `parity_dichotomy`                   | 四条件 + 不全偶 ⟹ **恰好** `ParityOK` 的两个分支之一                                                                  | 引理 4 的**精细化**：不只 `4                                                                                                                                                         |
| `pyth_halve` / `sol_halve`           | 全偶的四元组整体折半后仍是解                                                                                          | 折半下降。                                                                                                                                                                           |
| `parity_normal_form`                 | 任一解 ⟹ 存在同界 `Q' ≤ Q` 的解满足 `ParityOK`                                                                        | **过滤器的无损性**：程序砍掉的候选里不可能"丢掉最后一个解"。                                                                                                                         |
| **`no_sol_of_no_candidate`**         | `(∀ C A B, ¬ Candidate N C A B) ⟹ (∀ A B C D Q, IsSol N A B C D Q → False)`，**其中 `A = B` 情形作为显式假设 `hmid`** | **主定理**：程序输出 `solutions=0` ⟹ 共同分母 `Q ≤ N` 无解（离中线部分，无条件）。                                                                                                   |

`#print axioms` 实测（本人重跑）：

```
'RationalDistance.Algorithm.pyth_iff_div'          depends on axioms: [propext, Classical.choice, Quot.sound]
'RationalDistance.Algorithm.candidate_sound'       depends on axioms: [propext, Quot.sound]
'RationalDistance.Algorithm.exists_enum_of_sol'    depends on axioms: [propext, Quot.sound]
'RationalDistance.Algorithm.parity_dichotomy'      depends on axioms: [propext, Quot.sound]
'RationalDistance.Algorithm.parity_normal_form'    depends on axioms: [propext, Quot.sound]
'RationalDistance.Algorithm.no_sol_of_no_candidate' depends on axioms: [propext, Quot.sound]
```

**关于 `hmid` 与 `A ≠ B`：一次更正。** 主定理把 `A = B`（`x = 1/2`）作为显式假设 `hmid` 拿出来，`exists_enum_of_sol` 也带了 `A ≠ B`。上一版报告（以及 `Algorithm.lean` 的文件头注释）把这一点说成"反射对称性补偿不了的**真实覆盖缺口**"——**这是错的**，本次更正（§0 第 5 条）：遗漏的是**第二条对称性**，即对角线反射 `(A,B,C,D) ↔ (C,D,A,B)`（也就是 `(x,y) ↔ (y,x)`）。它把 `A = B` 的解重排成"数对为 `(C,D)`"的形态，而 `C ≠ D`（若 `C = D`，配上 `A = B` 就推出四个数全相等，于是 `A² + C² = 2A²` 是平方，与 `√2` 无理矛盾），所以引擎照常枚举到它。`notes/reduction.md` §6 从最初就是正确写法。

因此现状是：**定理为真，但 `hmid` / `A ≠ B` 这两条假设比需要的强**。要拿掉它们，Lean 里还需要一个算术引理——"`2A²` 不是正平方"（`√2` 无理的整数形式）——再把上面的交换补进去；这一步本次未完成，列入 §8.7 的局限。**这不影响任何计算结论**：`hmid` 恰好就是中线程序实测所验证的命题（`Q ≤ 2×10¹⁰` 无中线解），所以主定理的假设两条都由实际计算满足，头条结论（`Q ≤ 2×10¹⁰` 无解）成立且不依赖这个更正。

### 8.3 几何桥（Mathlib；`RationalDistanceAlgo/Bridge.lean`）

把 §1 的整数方程组与几何命题**严格**对应起来，这样 §8.2 的定理说的确实是几何问题：

```
RatDist x y  := IsSquare(x²+y²) ∧ IsSquare((1−x)²+y²) ∧ IsSquare(x²+(1−y)²) ∧ IsSquare((1−x)²+(1−y)²)
HasRatPoint N := ∃ Q A C, 0<Q ∧ Q ≤ N ∧ 0<A<Q ∧ 0<C<Q ∧ RatDist (A/Q) (C/Q)

theorem hasRatPoint_iff (N : ℕ) : HasRatPoint N ↔ ∃ A B C D Q, IsSol N A B C D Q
```

（`IsSquare a := ∃ b, a = b*b` 是 Mathlib 的定义。）证明中唯一的非初等步骤是

```
theorem nat_isSquare_of_rat_isSquare {n : ℕ} {q : ℚ} (h : q*q = n) : ∃ k : ℕ, k*k = n
```

即"平方为自然数的有理数必为自然数"。证明：写 `q = q.num / q.den`，平方去分母得
`q.num² = n * q.den²`，故 `q.den ∣ q.num²`；由 `Rat.reduced`（`q.num.natAbs` 与 `q.den` 互素）
得 `q.den = 1`，于是 `q` 是整数。

> **归属说明**：定理 1、2、3（§1）都是文献中的初等事实（见 §1 的归属说明与 §7）；本次新增的是
> 把它们**机器验证**并与搜索算法的谓词对接。

### 8.4 中线 `A = B` 的归约（Mathlib；`RationalDistanceAlgo/Midline.lean`）

本次把 §5 的中心线问题**重新归约到一个更干净的整数恒等式**（这是本次的新内容）：

```
theorem midline_key {A C D Q h1 h2 : ℤ}
    (hA2 : A + A = Q) (hCD : C + D = Q)
    (e1 : A*A + C*C = h1*h1) (e2 : A*A + D*D = h2*h2) :
    (C - D)^4 + 4 * Q^4 = 16 * h1^2 * h2^2
```

证明只需 `Q = 2A`、`C = 2A − D` 代入后 `ring`。数值自检（`A,C,D = 5,1,9`）：
`(1−9)^4 + 4·10^4 = 4096 + 40000 = 44096 = 16·26·106` ✓。

配合 `two_mul_sq_not_sq`（`2A²` 不是正平方，用经典下降证明，故 `C ≠ D`），得到：

> **一个中线解（`Q = 2A`）会给出非零整数 `x = C−D`、`y = Q`、`z = 4h1h2` 满足 `x⁴ + 4y⁴ = z²`。**

而"不存在非零整数解 `x⁴ + 4y⁴ = z²`"就是 **Fermat 直角三角形定理**（等价于 `FLT(4)`，
等价于"1 不是同余数"）。文件里把它显式命名为

```
def FermatRightTriangle : Prop := ∀ x y z : ℤ, x ≠ 0 → y ≠ 0 → x^4 + 4*y^4 ≠ z^2

theorem no_midline_of_fermat (hF : FermatRightTriangle) :
    ∀ N A C D : ℕ, IsSol N A A C D (2*A) → False
```

**状态说明（不夸大）**：Mathlib 有 `FLT(4)` 的对应版本（`fermatLastTheoremFour` /
`not_fermat_42 : a^4 + b^4 ≠ c^2`），但**没有**直接以 `x⁴ + 4y⁴ = z²` 形式给出的定理；
从前者推出后者需要与 `FLT(4)` 证明中同样的无穷下降。本次**没有**完成这一步，因此
`no_midline_of_fermat` 目前是"给定 Fermat 定理"的条件定理。本报告不把它算作"我们证明了中线无解"。

### 8.5 实现保真度交叉核对（核心 Lean；`RationalDistance/Count.lean`）

为了把 §8.2 的"关于谓词的定理"落到"关于**这个 C++ 程序**的结论"，另写了一个 Lean 旁证：
用**完全不同的算法**（对每个 `A ≤ N` 直接用 `Nat.sqrt` 判平方，而不是用 `C²` 的除数）实现同一套
计数器 `(partners, candidates, tested, solutions)`，再用 `native_decide` 机器核对它与
`fast_search3.cpp` 打印的数字**逐位相同**（第二轮又核对了 v4/v5 也逐位复现这三个三元组）：

```
example : stats 1000 = (2068,  3295,  774,   0) := by native_decide   -- C++: partners=2068 candidates=3295 tested=774 solutions=0
example : stats 2000 = (4642,  9293,  2111,  0) := by native_decide   -- C++: partners=4642 candidates=9293 tested=2111 solutions=0
example : stats 3000 = (7388,  16868, 3774,  0) := by native_decide   -- C++: partners=7388 candidates=16868 tested=3774 solutions=0
```

这类核对正是当初发现 `fast_search2.cpp` 截断 bug（伙伴表被截到 `N/2`）的那一类检验：伙伴集或循环
边界一旦写错，`partners`/`candidates`/`tested` 立刻不等。（对照：若把 `C` 的取值范围写成从 `0` 开始，
`partners` 恰好多出 `N`——这条也已被这次核对暴露出来并修正。）

### 8.6 主定理拼装（`RationalDistanceAlgo/Main.lean`）

```
theorem no_rational_point_of_no_candidate {N : ℕ}
    (h : ∀ C A B : ℕ, ¬ Candidate N C A B) (hF : FermatRightTriangle) :
    ¬ HasRatPoint N
```

以及一个把中线交给**计算**而不是交给 Fermat 定理的版本 `no_rational_point_of_no_candidate'`，
它接受假设 `hmid : ∀ M ≤ N, ∀ A C D, ¬ IsSol M A A C D (2*A)`——而这正是 §6 第 8 条
`src/midline_search.cpp` 在 `N = 2×10¹⁰` 上穷尽验证过的东西。

按 §8.2 末尾的更正，`hmid` 这条假设在数学上是**冗余**的（对角线反射已经覆盖 `A = B`），
所以这两个主定理都是"假设比需要的强"的版本，而不是有缺口的版本。

### 8.7 局限（逐条说清）

1. `native_decide` 依赖"可信编译/求值"（因为 `Nat.sqrt` 是良基递归，`decide` 无法归约），
   所以 §8.1 的 `Q ≤ 100/500` 与 §8.5 的计数器核对**不是纯内核证明**；`#print axioms` 会显示
   `Lean.ofReduceBool` 之类的求值公理。§8.2 的定理则不含这类公理（见上面的 `#print axioms` 输出）。
2. §8.4 的中线定理目前**以 Fermat 直角三角形定理为假设**；本次没有把它无条件化。
3. `exists_enum_of_sol` 的 `A ≠ B` 与 `no_sol_of_no_candidate` 的 `hmid` 是**多余**假设：
   两者都可以由对角线反射 `(A,B,C,D) ↔ (C,D,A,B)` 去掉，只差一个 Lean 算术引理
   "`2A²` 不是正平方"（`√2` 无理的整数形式；本次未完成）。**这是"证明多写了一行假设"，
   不是"结论有缺口"**——两处更正见 §0 第 5 条与 §8.2 末尾。
4. `no_sol_of_no_candidate` 说的是"**谓词** `Candidate` 无候选"——把 C++ 程序与该谓词对接
   靠的是 §8.5 的计数器核对（经验性）与 `pyth_iff_div`（数学性），不是把 C++ 代码本身形式化。
5. 共同分母 `Q = 2×10¹⁰` 的**头条搜索结果**本身仍是 C++ 计算，未在 Lean 中重跑（`native_decide`
   在该规模上不可行）。引擎与 Lean 模型的对接只在 `N ≤ 3000` 上做了逐位核对，外加 `N = 10⁴…10⁹`
   上"四套 C++ 实现互相对照"（§6 第 11 条）。

---

## 9. 结论、局限与后续方向

**本次已证（含文献已有与本次独立证明的），严格成立**

- 解的坐标必为有理数（定理 1；文献已有）；
- 问题与整数四元组方程组的严格等价（定理 2、3）；
- `3|Q`（引理 5；等价于 McCloskey 2019，文献已有）；
- `4|Q` 与本原奇偶模式（引理 4）、`12|Q`（定理 6）；其 2-adic 部分未在检索到的文献中发现。
- 上述 §4 的全部结论（含 `12|Q`）与 `Q ≤ 100` 的穷尽无解，均已在**核心 Lean 4** 中机器验证（§8）。

**本次已计算（穷尽搜索，可复现）**

- **内部点、共同分母 `≤ 2×10¹⁰` 无解**（第二轮；首轮到 `3×10⁹`），并**同日用同一二进制把界推到 `≤ 5×10¹⁰`，仍无解**（§3.2：`partners=420 219 661 420`、`solutions=0`、`sieve_overflow=0`，24 段分段跑，自身搜索时间合计 2.88 小时）。主搜索引擎的枚举**本身**就覆盖 `A = B`（见 §0 第 5 条的更正：靠对角线反射把数对换成 `(C,D)`），中线程序是一重独立的冗余验证（分别跑到 `Q ≤ 2×10¹⁰` 与 `Q ≤ 5×10¹⁰`，两次都是 0 个解）。两者合起来给出"共同分母 `≤ 5×10¹⁰` 无解"（§3、§5.1、§6 第 8、11、12 条）；
- 并有多套独立实现（v1/v2/v3/v4/v5）+ 独立暴力程序 + 独立枚举器 + 独立计数 + 打靶检验 + Lean 计数器核对的多重验证；
- 中心线情形在 `λ, u` 高度 `≤ 1500` 无候选点（与文献的"中心线不可能"一致）。

**未解——本报告不作任何越界断言**

- **问题的整体答案仍未解决**：本次既未找到点，也未证明不存在；
- **中线整体无解仍未被我们独立证明**：§8.4 的 Lean 归约把它精确化到 Fermat 直角三角形定理（`x⁴+4y⁴=z²`），这是一个经典定理（等价于 `FLT(4)`），但 Mathlib 里没有该形式，本次未完成从 `FLT(4)` 到该形式的无穷下降；因此中线结论目前是"计算到 `5×10¹⁰`"+"化归到已知定理"，不是独立证明；
- 我们的搜索界是**计算性**的，不构成不存在性的证明；共同分母 `> 5×10¹⁰` 的情形未被排除；
- 文献中"最大声称"（Roberts 的边长/坐标界）因抓取受阻而未核实，本报告不把它当作已确立的事实；
- 定理 6 的 2-adic 部分是否已有文献结论，我方的检索不足以断言。

**后续方向（按性价比）**

1. ~~用分段素数筛改写 v3，解除 `u16` 最小素因子筛对 `N < 4.29×10⁹` 的限制~~ **已完成（第二轮，v4/v5）**：分段余项筛 + 不构造 `C²` 的除数游走 + 按剩余类配对，见 §2.1；再加大素数优先的剪枝（v5），`N = 2×10¹⁰` 实测 76 分钟（12 线程），`N = 5×10¹⁰` 用分段跑（24 段，各段自身搜索时间合计 2.88 小时）。若要再往上走，下一步的瓶颈是除数游走的结点数（`N = 10⁹` 时 v5 走 4.4×10¹⁰ 个结点）与"排序 + 候选计数"（约 20% 时间），可行方向是给"素数个数 ≤ 3"的 `C` 写展开循环、免去递归与 `pwv` 间接寻址。
2. 把搜索扩展到**整个平面**（允许 `A`、`B`、`C`、`D` 取任意符号，即把当前的 `A+B = C+D` 推广为 `±A±B = ±C±D`）——这样结果可直接与文献"平面"提法比较。设 `a=|A|, b=|B|, c=|C|, d=|D| ≥ 1`，四条件只依赖绝对值，于是 `Q` 必须同时形如 `a+b` 或 `|a−b|` 与 `c+d` 或 `|c−d|`。令 `s = a+b`、`t = |a−b|`（`a≠b`），则候选 `d ∈ {c+s, c−s（若 ≥1）, t−c（若 ≥1）, c+t, c−t（若 ≥1）}`，再检验 `d^2+u^2` 对 `u∈{a,b}` 是否为平方即可。代价约为当前每对数检验次数的 5 倍。
3. 计算中心线四次曲线 Jacobi 簇的 Mordell–Weil 群（2-下降 + Selmer），以期**证明**中心线无解——这是一个有限可完成的目标（需要能算数域的类群/单位群，本机缺 CAS，可能需要自己实现或依赖在线计算）。
4. 对固定的 `x`（分母 `q` 有界）把椭圆曲线 `E_r`（MathOverflow 418260 的 `y² = x³ + (1/r²−1)x² − (2/r²)x + 1/r²`）的有理点集算全，给出比"共同分母有界"更强的覆盖。
5. 把 §8.4 的中线归约**无条件化**：在 Lean 里从 Mathlib 的 `not_fermat_42`（`a⁴+b⁴ ≠ c²`）推出 `x⁴ + 4y⁴ ≠ z²`（需要与 `FLT(4)` 证明中同样的无穷下降，或用 Sophie Germain 恒等式 `x⁴+4y⁴ = (x²−2xy+2y²)(x²+2xy+2y²)`）。
   5b. **（第二轮新增，优先级更高）把 `exists_enum_of_sol` 的 `A ≠ B` 与主定理的 `hmid` 去掉**：补一个 Lean 算术引理"`2A²` 不是正平方"（`A > 0`；可用"`A` 奇 ⟹ `2A² ≡ 2 (mod 8)` 与平方 mod 8 矛盾，`A` 偶 ⟹ 折半下降"证明，只需对强化命题 `∀ m ≤ A` 做普通归纳，不需要强归纳），再把对角线反射 `(A,B,C,D) ↔ (C,D,A,B)` 加进完备性证明（§0 第 5 条、§8.2）。做完之后主定理不再需要中线假设，中线程序纯属冗余验证。
6. 把 §8.5 的"计数器一致"升级为**关于程序本身**的结论：在 Lean 中把 `count.lean` 的 `Candidate` 谓词与 `stats` 函数之间的等价关系证明出来（即"计数器为零 ⟺ 没有候选"），从而把"程序输出 0"这一步也纳入形式化。

---

## 10. 复现方式与文件清单

```bash
cd <repository root>
bash run_all.sh quick                 # 几分钟：全部小型交叉核对（含 v3/v4/v5 逐位对照）
bash run_all.sh full                  # 数小时：1e9 / 3e9 各引擎 + 独立暴力到 1e4
bash run_all.sh deep                  # ~1.5 小时：头条运行 Q <= 2e10 + 中线 2e10 + 独立计数

# 或者一步步来
g++ -O3 -march=native -fopenmp -std=c++17 -o bin/fast_search5 src/fast_search5.cpp
./bin/fast_search5 --N 20000000000    # 头条：~1.5 小时，12 线程，峰值内存约 2.2 GB
./bin/midline_search 20000000000      # 中线独立穷尽：~26 秒
./bin/paircount 20000000000           # 独立伙伴对数（须等于 partners/2）
g++ -O2 -march=native -fopenmp -std=c++17 -o bin/fast_search3 src/fast_search3.cpp
./bin/fast_search3 --N 3000000000     # 旧引擎的 3e9 存档对照：25.7 分钟
g++ -O2 -std=c++17 -o bin/candcheck checks/candcheck.cpp
./bin/candcheck 3000                  # 四个计数器从定义出发（= fast_search5 --N 3000 --no-mod3）
python src/residue_structure.py       # 模剩余类结构
python src/center_line.py 1500        # 中心线归约 + 有理点搜索
```

（`bin/` 下的可执行文件与 `lean*/.lake/` 都不入库，见 `.gitignore`；`run_all.sh` 会重新构建它们。）

- `notes/reduction.md`：全部手写证明（定理 1–6、引理 4–5、搜索完备性论证 §6）。
- `notes/literature.md`：文献检索报告（逐条 URL，未核实项已标注）。
- `notes/lean-setup.md`、`notes/lean-formalisation.md`：Lean 环境与形式化。
- `notes/cost-5e10.md`：5e10 每段代价的实测分析（为什么贵在小 `C`、分段调度的依据、24 段的**实测**耗时表与逐段代价、对"约 6 小时"这一早期估计的书面更正、以及"可续跑运行为什么看起来像卡住"的判读；原始探针输出 `results/probe_cost_5e10.txt`）。
- `results/summary.md`、`results/*.txt`：全部原始数字与运行输出。注意**行尾**：程序在 Windows 上打印 CRLF，而 `.gitattributes`（`* text=auto eol=lf`）以 LF 存储与检出，所以重跑得到的 stdout 与仓库里的存档只差 `\r`；`checks/verify_results.py` 与 `checks/verify_chunking.sh` 在比较前都会先去掉 `\r`（`verify_chunking.sh` 的第一版正因为这一点报过一次假的 DIFFERS）。
- `src/*`：v1/v2/v3/v4/v5 搜索引擎、独立暴力程序、独立枚举器、独立计数、模结构脚本、中心线脚本。
- `lean/`：核心 Lean 形式化项目（无 Mathlib）：`ModArith/Parity/TwelveDvd`（`12|Q`）、`Algorithm`（**算法正确性**）、`Count`（**计数器交叉核对**）、`Search`、`BigCheck500`。
- `lean-mathlib/`：Mathlib 层：`RationalDistanceAlgo/Bridge`（**几何桥**）、`Midline`（**中线归约**）、`Main`（主定理拼装）。`lakefile.toml` 用 `srcDir = "../lean"` 复用核心源码。
- `src/midline_search.cpp`：**中线（`A=B`）独立穷尽搜索**（第二轮做了 128 位改写）；`results/midline_N2e10.txt` 是其输出。
- `src/fast_search4.cpp` / `src/fast_search5.cpp`：**第二轮的新引擎**（分段余项筛、不构造 `C²` 的除数游走、按剩余类配对；v5 再加大素数优先剪枝）。`results/run_N2e10.txt`、`results/paircount_N2e10.txt` 是头条运行的输出与独立对照。
- `checks/`：**从定义出发的独立核对程序**，每一个都重新推导报告里的一个计数器——`sievecheck.cpp`（分段筛分解 vs 试除，在 `M = 2×10⁶` 上 `checked=1999999 mismatches=0`）、`partners_ref.cpp`（`Σ_C |T(C)|` 与 `max_partners_per_C` 的暴力真值）、`candcheck.cpp`（`partners/candidates/tested/solutions` 四个计数器，也正是 `lean/RationalDistance/Count.lean` 里 `native_decide` 核对的那一组）、`midline_ref.cpp`（中线枚举器的阳性对照，`Amax=50000` 时 `7312 = 7312`）。`ref_T_v3.txt` / `ref_T_v4.txt`：`N = 200` 时 v3 与 v4 的 `T(C)` 全表输出（v3 对每个 `C ≤ 199` 各一行、空集也打；v4 只打非空集）。两者的 **124 个非空条目逐字相同**，且 v4 唯一省略的 76 个 `C` 正是 v3 中 `T(C)` 为空集的那些；v4 文件末尾还附了引擎在 `N = 200` 的计数器（`partners=302 max_partners_per_C=10 candidates=234 tested=24 solutions=0`）。
- `explore/`：**第一轮最早的独立交叉核对**（Python）。`extsearch3.py` 用 `(z−w)(z+w) = n⁴` 的除数分解枚举 `y`，在共同分母 `≤ 2000` 上给出 0 解（`explore/extsearch3_run.log`），是 `notes/literature.md` §3.3 引用那一条。已被 `src/brute.cpp` 取代，不属于 `run_all.sh`。
- 仓库根：`README.md`（英文入口）、`LICENSE`（MIT）、`.gitignore`、`.gitattributes`、`REPORT.md`（本文件）、`run_all.sh`（`quick`/`full`/`deep`/`5e10`，含上面全部核对与两个 Lean 项目的构建）、`run_5e10.sh`（§3.2 的分段可续跑 5e10 运行脚本）。
- `checks/verify_chunking.sh`：**证明 `--seglo`/`--seghi` 分段模式与整跑等价**——`N = 10⁸` 整跑 vs 三段之和逐位相同，`10⁸`/`10⁹` 整跑与 v3 存档逐位相同（只 `divisor_steps` 更小）。输出存档在 `results/verify_chunking.txt`，它捕获的每次运行的原始 stdout/stderr 留在 `results/.verify/`（`a.*` 是 `10⁸` 整跑、`b.*` 是三段及其求和、`c.*` 是 `10⁹` 与 v3 存档的对照）。
- `results/run_5e10.log`：`run_5e10.sh` 那次真实运行（`bash run_5e10.sh 50000000000 24 > results/run_5e10.log`）的**驱动日志**——每段一行，含段区间、该段耗时与它的输出行；`notes/cost-5e10.md` 的实测耗时表就是从这里读出来的。脚本本身不写这个文件，它是调用方重定向的产物，保留下来作为逐段计时的原始出处。
- `results/chunks5e10/`：5e10 分段运行的**逐段原始输出**（`part_kk.txt` 是 stdout，`part_kk.err` 是进度；`pieces.txt` 是这 24 行的拼接，供脚本逐字段求和；`bounds.txt` 是段界）；`results/attempt1_5e10_interrupted.err` 是第一次整跑被断电打断时的进度日志（stdout 为空，见 §3.2）。
- `checks/verify_results.py`：**把 `results/` 里记录下来的每个数字互相对照的脚本**（不重算任何搜索，只读已归档的原始输出）：**20 项断言全部 PASS 时退出码 0**（含 5e10 的 4 项），输出存档在 `results/verification.txt`。它覆盖上面提到的每一条交叉核对（v3/v4/v5 逐位一致、四个计数器与引擎一致、`partners = 2 × paircount`、中线阳性对照 7312、`T(C)` 两张表、5e10 分段求和与独立计数、报告引文与原始输出逐字一致、两个 Lean 项目构建成功）。
- `checks/scan_leaks.py`：**发布前的隐私核对**——扫描整棵树（按字节读，因此二进制/编码都不会掩盖命中）看是否残留**本机绝对路径**（盘符路径、UNC、`/d/...` 形式的 MSYS 路径、`/home`、`/Users`）、**本机目录名**（运行时从被扫描的根目录名读出，所以脚本自身不含这个字符串）或**机器名前缀**（`DESKTOP-`/`LAPTOP-`/`WIN-`）；全树干净时退出码 0，否则列出文件与行号并退出 1。它对 `notes/` 里 `<repo>` / `<elan>` 这两个占位符负责：正文中所有本机绝对路径都已改写为占位符（`notes/lean-setup.md`、`notes/lean-formalisation.md`、`notes/lean-parity-12dvd.md`，以及 `results/lean_verification.txt`——后者的第 2 行明确标注该日志的路径已做脱敏、其余内容一字未改），该脚本保证脱敏后不再有路径渗回来。也已接入 `run_all.sh quick` 的最后一步。
- `_third_party/`：**他人材料，不属于本次发布**（下载的论文 HTML/文本与 Stack Exchange API 响应、以及一份前作归档与其 Lean 陈述）。已在 `.gitignore` 中排除，检索出处见 `notes/literature.md`，说明见 `_third_party/README.md`。

---

## 11. 作者声明：全部工作由 DeepSeek v4.1 flash 独立完成

**本仓库的一切成果都由 AI 研究助理 Agent「DeepSeek v4.1 flash」独立完成，没有人类合作者。** 具体到每一类产物：

1. **算法设计**：`notes/reduction.md` 里的全部归约与手写证明（定理 1–6、引理 4–5、搜索完备性论证）、把几何问题化成整数方程组的那一步、`12 | Q` 的结构定理、以及 §2 描述的搜索引擎（分段余项筛、不构造 `C²` 的除数游走、按剩余类配对、素数从大到小的剪枝顺序）——全部由该 Agent 自行推导、自行设计。
2. **代码**：`src/` 下 v1–v5 各版引擎、独立暴力程序 `brute.cpp`、独立枚举器、独立计数 `paircount.cpp`、128 位改写的 `midline_search.cpp`、模结构脚本；`checks/` 下每一个"从定义出发"的独立核对程序；`run_all.sh`、`run_5e10.sh` 两个驱动脚本；以及 `checks/verify_results.py`、`checks/verify_chunking.sh` 这两个交叉核对脚本、发布前扫描本机路径残留的 `checks/scan_leaks.py`——全部由该 Agent 编写、调试、修 bug。设计过程中发现并更正的错误（包括本报告 §0 第 5 条对上一版"漏掉中线"这一错误说法的**书面撤回**）也全部由它自己发现并记录。
3. **Lean 4 形式化**：`lean/` 全部模块（含算法**可靠性与完备性**的机器验证、用 `native_decide` 逐位核对 C++ 计数器的 `Count.lean`）与 `lean-mathlib/` 全部模块（几何桥 `Bridge`、中线归约 `Midline`、主定理拼装 `Main`），包括工具链与 Mathlib 的安装配置——全部由该 Agent 完成。
4. **文献检索**：`notes/literature.md` 的逐条 URL 检索、对未核实项的标注（例如被 JS 反爬挡住、无法取到正文的那条声称）、以及对 viXra 上那份"证明"的不可信判定——由该 Agent 独立完成。
5. **数值计算**：`results/` 下记录的**每一次运行**（1e8/1e9/3e9/2e10 头条、独立计数、中线、分段 5e10、全部探针与核对）都是该 Agent 在本机上自己启动、自己监控、自己读结果、自己交叉核对的；包括电脑意外断电后自行诊断出"stdout 块缓冲导致计数器丢失"这一原因，并据此设计出可续跑的分段模式。
6. **文档**：本报告、`README.md`、`results/summary.md`、`notes/*.md` 以及 `results/` 里的全部原始输出归档，均由该 Agent 撰写与整理。

人类的介入**仅限于四件事**：提出要推进的问题（"单位正方形内部是否存在到四顶点距离均有理的点"）、提供本机与工具链（Windows 笔记本，13th Gen Intel Core i5-13420H，8 核 12 线程，g++/elan/Python）、规定工作纪律（不得编造任何成果、不得放弃、每个数字都要能追到原始输出）、以及**由人类自己用 GPG 对发布提交签名**。没有任何人类撰写的数学内容、代码或结论进入本仓库；也没有人类对任何结论做过修改或更正。

因此：本仓库的所有功劳与所有责任都归于该 Agent 及其运行配置。若发现任何错误，错误同样由它产生——报告中的每一处更正都留有原始出处，可逐条追溯。
